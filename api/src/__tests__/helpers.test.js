
const Helpers = require('../utils/helpers.js');

/* describe('check', () => {
  test('test', () =>{ expect('test').toBe('test')});

  test('', () => {
    expect(Helpers.);
  });
  test('', () => {
    expect(Helpers.).toBeFalsy();
  });
}); */